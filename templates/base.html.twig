<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <title>
      {% block title %}Welcome!{% endblock %}
    </title>
    {% block stylesheets %}{{ encore_entry_link_tags("app") }}{% endblock %}
    {% block javascripts %}{{ encore_entry_script_tags("app") }}{% endblock %}
  </head>
  <body>
    <nav>
      {% if is_granted('ROLE_USER') %}
        Hi, {{ app.user.name }}
        <img src="{{ app.user.photo }}" alt="avatar" width="45" height="45">
      {% endif %}
    </nav>
    {% block body %}{% endblock %}
  </body>
</html>
